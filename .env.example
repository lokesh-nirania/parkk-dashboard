# Supabase → Project Settings → Data API  (and API Keys for the anon key)
# Both are safe in the browser: the anon key only ever gets what RLS allows.
NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR-ANON-KEY
